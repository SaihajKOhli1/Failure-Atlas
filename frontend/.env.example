# Failure Atlas Frontend - Environment Variables
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to git (it's in .gitignore)

# ============================================================================
# Backend API Configuration
# ============================================================================
# Backend API base URL
# Default: http://127.0.0.1:8000 (for local development)
# For production: https://api.yourdomain.com (or your backend URL)
# Railway: Set to your Railway backend URL if backend is a separate service
#          If backend and frontend are on same service, can use relative URLs
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Note: NEXT_PUBLIC_* variables are exposed to the browser
# DO NOT put secrets here - only public configuration values
